using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class daHUI : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		//если растояние до объекта < радиуса навмешабстейкл создается вектор от объекта к агенту который умножается на радиус и в эту точку строится calculatepath
		//иначе калькулейт пас к главтрансу
	}
}
